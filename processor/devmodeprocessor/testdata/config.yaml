receivers:
  nop:

processors:
  devmode:


exporters:
  nop:

service:
  pipelines:
    logs:
      receivers: [nop]
      processors: [devmode]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [devmode]
      exporters: [nop]
    traces:
      receivers: [nop]
      processors: [devmode]
      exporters: [nop]
